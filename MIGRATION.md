# 迁移指南

## 🚨 破坏性更改：移除默认管理员账户，改为首个注册用户自动成为超级管理员

**影响版本**：`>= 1.0.0`（commit `93c1627` 及之后）

### 变更概述

| 项目 | 变更前 | 变更后 |
|------|--------|--------|
| 默认账户 | 数据库初始化时自动创建 `admin/admin123` 和 `user/user123` | 不再自动创建任何账户 |
| 首次注册 | 需要先获取图形验证码 + regToken | **免验证码**，直接注册，自动成为超级管理员（`role='admin'`、`id=1`） |
| 后续注册 | 需要图形验证码 + regToken | 不变 |
| 超级管理员删除 | 所有 `role='admin'` 的用户均不可删除 | 仅 `id=1` 的超级管理员不可删除，其他管理员可正常删除 |

### 对现有系统的影响

- **已有用户的系统**：完全透明，已存在的 `admin` 和普通管理员账户不受影响，继续正常工作。
- **全新部署**：数据库初始化后没有任何用户，需要手动注册第一个账户作为超级管理员。

### 迁移步骤（全新部署）

1. 启动服务器
2. 调用 `POST /api/auth/register`，**无需传 regToken**：
   ```json
   {
     "username": "your_admin",
     "password": "your_password"
   }
   ```
3. 返回 `{ "message": "超级管理员注册成功", "role": "admin" }`
4. 使用该账户登录即可管理整个系统

### 迁移步骤（已有系统升级）

如果已经存在用户（包括之前的 `admin` 账户），系统正常运行，无需任何迁移操作。

如果希望将已有数据库中的首位用户提升为不可删除的超级管理员身份，可以通过 SQLite 直接操作：

```sql
-- 查看首位用户（id=1）
SELECT id, username, role FROM users WHERE id = 1;

-- 确认其 role 为 admin（如果不是则需要更新）
-- 其他操作无需变更
```

### 需要更新的集成

如果您的自动化脚本或 CI/CD 依赖 `admin/admin123` 默认账户进行 API 测试：

1. 启动服务器后先调用注册接口创建管理员
2. 使用注册的管理员账户进行后续测试
